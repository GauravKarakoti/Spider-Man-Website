// Add a little interactivity
document.querySelectorAll('.extra-images').forEach(img => {
    img.addEventListener('click', () => {
        alert('This is an image of Spider-Man history!');
    });
});
